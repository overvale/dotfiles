#!/bin/zsh

tmpSafariURL=$(safariurl)
textAppend="

<a href='$tmpSafariURL'>$tmpSafariURL</a>"

if [ $@ ]; then
    python -m readability.readability -u $tmpSafariURL | tail -n +2 > ~/Desktop/$@.html && \
    echo $textAppend >> ~/Desktop/$@.html
else
    echo "Requires argument"
fi
