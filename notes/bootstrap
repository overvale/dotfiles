# Use this document to setup a new Mac.
# This is not meant to be run as a shell-script, rather, as a guide.

# Sign in with AppleID

# Check for software updates
# Install ad blocking - I currently use 1Blocker
# Change caps-lock to control

# Copy files from backup
# Install ~/dev from backup
# pull-down your dotfiles, or copy from backup

# Change your shell to zsh
chsh -s /bin/zsh

# Installs the developer command line tools
xcode-select --install

# Add a welcome message to your terminal
sudo echo "Welcome to UNIX!" > /etc/motd

# Set up ssh keys

# install homebrew - worth checking homepage for, potentially, updated instructions
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

brew install aspell
brew install emacs --with-cocoa
brew install exa
brew install fzf
brew install git
brew install ispell
brew install pandoc
brew install par
brew install the_silver_searcher
brew install tldr
brew install tree
brew install vim
brew install zsh

# Install princeXML
# make install textplay (symlink)

mkdir ~.config
ln -s ~/dev/Karabiner .config/karabiner

# Symlink a bunch of things - run from $HOME
ln -s ~/Library/Mobile\ Documents/com~apple~CloudDocs/oht cloud;
ln -s ~/dev/emacs ~/.emacs.d;
ln -s ~/cloud/src/dot/vim .vim;
ln -s ~/cloud/src/dot/git/gitconfig .gitconfig;
ln -s ~/cloud/src/dot/git/gitignore_global .gitignore_global;
ln -s ~/cloud/src/dot/zsh/zshrc ~/.zshrc
ln -s ~/cloud/src/dot/zsh/zshenv ~/.zshenv

# Install vim plugin manager + plugins
# Reconstruct ~/.marks directory

# Install apps:

# 1Blocker
# 1Password7
# Adobe Lightroom
# Arq
# BBEdit
# Fantastical
# Finda
# Front and Center
# Gitup
# Google Chrome
# Karabiner
# Keyboard Maestro
# Microsoft Office
# Monodraw
# Name Mangler
# NetNewsWire
# Pixelmator
# Slack
# SuperDuper
# Tot
# Transmit
# ViDL

# Install fonts from server
