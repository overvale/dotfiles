" General app prefs
" ---------------------------------------------------------------------------

set nocompatible                  " Must come first because it changes other options.
syntax enable                     " Turn on syntax highlighting.
filetype plugin indent on         " Turn on file type detection.
call pathogen#infect()            " Load Pathogen plugin
set modelines=0                   " Turn off modeline support

set encoding=utf-8                " Set default encoding
set ttyfast                       " Tell vim you're using a fast terminal.

set title                         " Set the terminal's title
set visualbell                    " No beeping.
set nobackup                      " Don't make a backup before overwriting a file.
set nowritebackup                 " And again.
set directory=$HOME/.vim/tmp//,.  " Keep swap files in one location

set hidden                        " Handle multiple buffers better.
set wildmenu                      " Enhanced command line completion.
set wildmode=list:longest         " Complete files like a shell.

set ignorecase                    " Case-insensitive searching.
set smartcase                     " But case-sensitive if expression contains a capital letter.
set incsearch                     " Highlight matches as you type.
set hlsearch                      " Highlight matches.
set showmatch                     " Show matching brackets, etc.

set gdefault                      " Sets searches to be global (line) by default

set autoread                      " Tells vim to auto-read changes to the file from the disk.


" Editing Prefs
" ---------------------------------------------------------------------------
set autoindent                    " Turn on auto-indenting
set tabstop=4                     " Tab preferences
set shiftwidth=4
set softtabstop=4
set expandtab                    " Use spaces instead of tabs
set backspace=indent,eol,start    " Intuitive backspacing.

" Turn on spell-checking
"setlocal spell spelllang=en_us

" Turn the blinking cursor off in normal mode
set gcr=n-c-v:blinkon0


" Display Stuff
" ---------------------------------------------------------------------------

" Useful status information at bottom of screen
set statusline=[%n]\ %<%.99f\ %h%w%m%r%y\ %{exists('*CapsLockStatusline')?CapsLockStatusline():''}%=%-16(\ %l,%c-%v\ %)%P
set laststatus=2                  " Show the status line all the time
set showcmd                       " Display incomplete commands.
set showmode                      " Display the mode you're in.
hi StatusLine ctermfg=15 ctermbg=0
hi StatusLineNC ctermfg=8 ctermbg=0


"set number                        " Show absolute line numbers.
set relativenumber                " Show relative line numbers.
set ruler                         " Show cursor position.
"set cursorline                    " Highlight the line the cursor is on

" word-wrapping setup stolen from drbunsen
set formatprg=par
setlocal wrap 
setlocal linebreak

set scrolloff=3                   " Show 3 lines of context around the cursor at all times.
set background=dark
colorscheme base16-ocean

" Height of the command bar
set cmdheight=2

" show 'noname' buffers in bufExplorer 
let g:bufExplorerShowNoName=1


" Vundle Stuff
" ---------------------------------------------------------------------------

filetype off                  " required!
set rtp+=~/.vim/bundle/vundle/
call vundle#rc()
" let Vundle manage Vundle
" required! 
Bundle 'gmarik/vundle'


" Vundles...
Bundle 'scrooloose/nerdtree'
Bundle 'matze/vim-move'
Bundle "sjl/gundo.vim"
Bundle "tpope/vim-markdown"
Bundle 'jlanzarotta/bufexplorer'

filetype plugin indent on     " required!
"
" Brief help
" :BundleList          - list configured bundles
" :BundleInstall(!)    - install (update) bundles
" :BundleSearch(!) foo - search (or refresh cache first) for foo
" :BundleClean(!)      - confirm (or auto-approve) removal of unused bundles
"
" see :h vundle for more details or wiki for FAQ
" NOTE: comments after Bundle commands are not allowed.



" Shortcuts
" ---------------------------------------------------------------------------

" Disable The Arrow Keys - Oh God...
nnoremap <up> <nop>
nnoremap <down> <nop>
nnoremap <left> <nop>
nnoremap <right> <nop>
inoremap <up> <nop>
inoremap <down> <nop>
inoremap <left> <nop>
inoremap <right> <nop>
nnoremap j gj
nnoremap k gk

" Clear search highlighting
nnoremap <leader><space> :noh<cr>

" Open .vimrc in a vertical split
nnoremap <leader>erc <C-w><C-v><C-l>:e $MYVIMRC<cr>

" Quick-reload of this file
nnoremap reload :source $MYVIMRC<cr>

" Open NERDTree
nnoremap <leader>nt :NERDTreeToggle<cr>

" Gundo
nnoremap <F5> :GundoToggle<CR>

" When moving to the end of the document also move to the end of the line
nnoremap G G$

" Indentation remaps
nnoremap > gv>
nnoremap < gv<

nnoremap mdown :set syntax=markdown<cr>

" Vim-Move
let g:move_key_modifier = 'C'
