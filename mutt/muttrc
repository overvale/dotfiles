source ~/code/private-dotfiles/muttrc

set spoolfile = '+INBOX'
set postponed = '+[Google Mail]/Drafts'
set header_cache =~/.muttfiles/cache/headers
set message_cachedir =~/.muttfiles/cache/bodies
set certificate_file =~/.muttfiles/certificates

set realname = 'Oliver Taylor'
set mark_old=no
set sort = threads
set fast_reply
set include
set nomark_old
set nomarkers

set sig_on_top
set signature = '~/code/dotfiles/mutt/signature'



# Open urlview
macro pager \Cu "|urlview<enter>" "call urlview to open links"

# Use `contacts` for email autocomplete
set query_command = "contacts -Sf '%eTOKEN%n' '%s' | sed -e 's/TOKEN/\t/g'"
bind editor <Tab> complete-query
bind editor ^T    complete




# Index View Options ---------------------------------
set date_format = "%m/%d %H:%M"
set index_format = "[%Z]  %D  %-20.20F  %s"
set sort_aux = reverse-last-date-received  # like gmail
set uncollapse_jump                        # don't collapse on an unread message
set sort_re                                # thread based on regex
#set reply_regexp = "^(([Rr][Ee]?(\[[0-9]+\])?: *)?(\[[^]]+\] *)?)*"

bind index gr imap-fetch-mail
bind index gg       first-entry
bind index G        last-entry
macro index <esc>g "<shell-escape>open http://gmail.com<enter>" "Open Gmail in browser"
bind index R        group-reply
bind index <space>  collapse-thread

#set quote_regexp = "^( {0,4}[>|:#%]| {0,4}[a-z0-9]+[>|]+)+"
alternative_order text/plain text/enriched text/html

# Pager Key Bindings ---------------------------------
# bind pager k  previous-line
# bind pager j  next-line
# bind pager gg top
# bind pager G  bottom

# bind pager R  group-reply

# View attachments properly.
bind attach <return> view-mailcap










source ~/code/dotfiles/mutt/rookie.mutt

# color  normal	     brightwhite     black
# color  attachment    brightyellow    black
# color  hdrdefault    brightyellow    black
# color  indicator     black           blue
# color  markers	     brightred       black
# color  quoted	     green           black
# color  signature     cyan            black
# color  status	     black           yellow
# color  tilde	     blue            black
# color  tree	         white           black
