# Fix tmux's broken pasteboard interaction
set -g default-command "reattach-to-user-namespace -l zsh"

# improve colors
set -g default-terminal "screen-256color"
set -g status-bg colour10
set -g status-fg colour08
set -g window-status-current-style fg=colour01

# Customize the statusbar
set -g status-left '[#S]'
set -g status-right 'help: ⌥t? #[fg=colour01][tmux]'

# remap prefix to Meta + t
set -g prefix M-t
unbind C-b
bind M-t send-prefix

# use vi bindings
setw -g mode-keys vi

# Reload the config.
unbind r
bind r source-file ~/.tmux.conf \; display "Reloaded ~/.tmux.conf"
